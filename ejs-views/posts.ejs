<!DOCTYPE html>
<html lang="en">
    <%- include ('./partials/head.ejs') %>
<body>
    <%- include('./partials/nav.ejs') %>
    <ul>
        <% if (posts.length) { %>
            <% posts.forEach(({title, author, text, date, id}) => { %>
                <li>
                    <a href='/posts/<%= id %>'><%= title %></a>
                </li>
                <h3><%= author %></h3>
                <h3><%= date || "No date" %></h3>
                <p><%= text %></p>
           <% }) %>
           <% } %>
    </ul>
</body>
</html>